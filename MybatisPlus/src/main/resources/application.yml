# 数据库连接配置 mysql 8 要加时区的配置  serverTimezone=GMT%2B8
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?serverTimezone=GMT%2B8&useUnicode=true&charaterEncoding=UTF-8
    username: root
    password:

# 配置日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl